CREATE TABLE IF NOT EXISTS DeletedPasswords (
    id TEXT NOT NULL PRIMARY KEY
);

insert:
INSERT INTO DeletedPasswords (id) VALUES (?);

getAll:
SELECT id FROM DeletedPasswords;

delete:
DELETE FROM DeletedPasswords WHERE id = ?;